import { displayProfile } from "../../handlers/auth/displayProfile.js";

/**
 * Initializes the profile page by displaying the user's profile and related data.
 */
export function initProfile() {
  displayProfile();
}
